<%= form_with(model: rent, local: true) do |form| %>
  <% if rent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rent.errors.count, "error") %> prohibited this rent from being saved:</h2>

      <ul>
      <% rent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Книга %>
    <%= form.text_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :Абонент %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :Дата_выдачи %>
    <%= form.date_select :give %>
  </div>

  <div class="field">
    <%= form.label :Дата_возврата %>
    <%= form.date_select :giveback %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
